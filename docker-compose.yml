services:
  api:
    build: .
    restart: unless-stopped
    environment:
      DOCDB_LMDB_PATH: /data/docdb.lmdb
    volumes:
      - lmdb_data:/data:ro
    expose:
      - "8000"

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - api

volumes:
  lmdb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /srv/docdb
